3.1 기본 데이터 타입(숫자,불린값,문자열,null,undefnied)
    모든 숫자는 실수로 취급
    문자열 생성시 수정 불가능
    null은 조건문을 이용시 ===연산자 이용
3.2 참조데이터 타입 - 객체
    객체 생성   
        1. 객체 함수 이용 - new 객체()
        2. 객체 리터럴 방식 - 객체 = {객체 프로퍼티 값: value값}\
    객체 표기
        1.객체 프로퍼티 추가시 기존거면 갱신되고, 
          아니면 새로운 프로퍼티 추가하여 값을 할당 
        2. -(빼기 연산자)가 들어간 프로퍼티는 
           무조건 대괄호([])표기법을 써야함
    객체 출력 - for in 문 사용
        foo = {
                name : adf,
                age:30,
            }
        var = a;<br>
        for(a in foo){
            console.log(a,foo[a]);
        };
        results
            name foo,
            age 30,
            major engineering
    객체 프로퍼티 삭제 
         1.delete연산자 
         2.객체 자체는 삭제 할 수 없음